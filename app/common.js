'use strict';

module.exports = {
		// Which filter are we using?
		TodoFilter: '', // empty, active, completed

		// What is the enter key constant?
		ENTER_KEY: 13,
		ESCAPE_KEY: 27,
		API_PROTOCOL: 'http://',
		API_HOST: 'localhost',
		API_URL: 'http://localhost',
		DB: {}
	};

	console.info('common.js initialized');
